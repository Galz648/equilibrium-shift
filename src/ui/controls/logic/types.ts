/** Heater slider group (root contains slider; heat value is mirrored into metrics). */
export type HeaterTargets = {
    root: HTMLElement;
    slider: HTMLInputElement;
    heatValue: HTMLElement;
};
